#include <bits/stdc++.h>
using namespace std;
const int N = 10;
int grid[N][N];

int n;

void remove() {
    for(int i = 0;i < n;++i) {

    }
}


void dfs(int idx) {
    
} 

int main() {
    cin >> n;
    for(int i = 0;i < 5;++i) {
        int t, j = 0;
        while(cin >> t && t) {
            grid[i][j++] = t;
        }
    }
    dfs(0);
    return 0;
}