#include <bits/stdc++.h>
using namespace std;
int n, m, ans = INT_MAX;

void dfs(int idx, int r, int h, int v) {

}

int main() {
    cin >> n >> m;
    int vmi = n / m, vma = n;
    dfs(0, 0, 0, m);
    cout << ans << "\n";
    return 0;
}